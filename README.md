# AI 教学与分享 · 评论洞察工作台

面向 **AI 教学与知识分享** 方向的评论采集与分析应用：从真实评论中发现需求、卡点与内容机会，并把结论转化为**可审核的回复**与**内容选题**。提供 Web 界面，支持多平台视频/笔记评论抓取，以及：

- **评论洞察**（默认证据分析引擎）：微批次证据抽取 → 代码校验 → 数据集研究 Agent → 可读报告；
- **评论区回复**：算法筛选值得回复的用户 → 人工确认 → AI 按用户意图生成回复 → 人工审核 → 受控回复；
- **内容选题**：把评论需求转成选题并生成多形态草稿。

> 核心边界：**AI 只负责发现与起草，人负责确认与发送。** AI 生成环节必须人工点击开始；回复只发送人工审核通过的内容，并受频率与时段约束，可随时停止。

> **项目归档与交接（跨会话必读）见 [`docs/项目归档与交接.md`](docs/项目归档与交接.md)**

## 功能

- **Web 采集界面**：选择平台 → 粘贴链接或扫描博主视频 → 批量采集评论
- **评论洞察（默认 `evidence_items_v1`）**：
  - 选择已采集 CSV → 配置 DeepSeek 等 API → 分析 **100 / 1K / 10K / 全部** 条
  - 微批次证据提取 → 代码校验 → 数据集研究 Agent → `research_report.md`
  - 断点续跑、预算上限、停止/继续、失败重试、大任务二次确认
  - 潜在用户聚合与评分、公开评论回复
  - 导出分析 CSV、洞察报告、候选用户与回复记录
- **评论区回复**（人工确认 → AI 生成 → 人工审核 → 受控回复）：
  - 纯算法筛选值得回复的用户，人工勾选确认
  - AI 按每位用户的评论意图逐条生成回复内容（不复用模板）
  - 逐条审核「通过 / 不回复」，仅审核通过的内容进入发送队列
  - 「开始回复」按**最小间隔 + 随机抖动 + 每日上限 + 允许时段**受控发送，可随时停止
- **内容选题**：代码统计需求 → AI 生成选题与多形态草稿 → 人工审核后再发布
- **创作者排行**：扫描博主全部视频，按评论数排序，多选后一次采集
- **分视频保存**：每条内容独立子文件夹（`data/` 下按分类/博主/视频组织）
- **多平台支持**：B 站、抖音、快手、小红书等（日常 MVP 以 B 站验证最多；自动回复目前仅 B 站）

## 快速开始

### 环境要求

- Python 3.11+
- **[uv](https://github.com/astral-sh/uv)**（推荐）
- Google Chrome / Chromium（用于登录与采集）

### 安装

```bash
git clone https://github.com/xiaohuanglian/video-comment-analyzer.git
cd video-comment-analyzer
uv sync
uv run playwright install chromium
```

### 启动 Web 界面

```bash
./run_web.sh
```

浏览器打开 http://127.0.0.1:8766 → Tab **「2 评论洞察」**

### 评论洞察使用说明

1. **选择文件**：在左侧树中勾选 `data/**/comments_*.csv`
2. **配置 API**：Base URL（如 `https://api.deepseek.com`）、Model（默认 `deepseek-v4-flash`）、API Key  
   Key 仅保存在浏览器 sessionStorage，**不会写入磁盘**
3. **分析数量**：100 / 1K / 10K / 全部；默认 100 条适合验收，超过 2000 条会二次确认预算与耗时
4. **开始分析**：创建任务并后台运行（**默认证据分析引擎**）；未完成时可「继续分析」
5. **历史任务**：下拉加载已有任务；**选新文件会创建新任务**，不会与历史任务合并；旧任务按原 `config.json` 读取
6. **高级回退**：模型面板「高级设置」可选「旧版逐条分析」，仅作用于该新任务
7. **预算暂停**：达到预算上限后提高上限并点「继续分析」；已完成条目不重复计费
8. **下游步骤**（均需手动触发）：
   - 「评论区回复」：筛选用户 → 人工勾选确认 →「生成回复内容」→ 逐条审核 →「开始回复」（受控）
   - 「内容选题」：基于评论需求生成内容选题与多形态草稿 → 人工审核后再发布
9. **导出**：分析完成后自动写入 CSV 同目录（`{任务名}_分析结果.csv`、`_洞察报告.md`）；生成回复/内容选题后也会自动保存对应文件

### 评论区回复（人工确认 → AI 生成 → 人工审核 → 受控回复）

1. **关联任务**：选择前两板块产出的分析任务，无需重新上传 CSV
2. **筛选**（纯算法）：点击「筛选值得回复的用户」
3. **人工确认**：勾选确认要回复的用户
4. **生成内容**（需 API Key）：点击「为勾选用户生成回复内容」，AI 按用户意图逐条生成
5. **人工审核**：逐条点击「审核通过 / 不回复」，并可编辑文本
6. **开始回复**（受控）：设置最小间隔、随机抖动、每日上限与允许时段后点击开始，可随时停止；只发送审核通过的内容

> 自动回复目前仅支持 **B 站**，需在服务端配置登录 Cookie：环境变量 `VCA_OUTREACH_COOKIE`（或复用爬虫的 `COOKIES`）。其他平台暂不自动发送，保留手动复制。

分析任务保存在：
- **新任务**：`{CSV 所在目录}/.insight/{任务名}/`
- **旧任务（兼容）**：`data/analysis_runs/{任务名_日期}/`
- **产物**：`evidence_cards.jsonl`、`research_analysis.json`、`research_report.md`、`candidates.json`、`outreach.json`、`content_plan.md`

> 本工具默认仅本机使用（`run_web.sh` 绑定 `127.0.0.1`）。API **无鉴权**；请勿将端口暴露到公网。

### 命令行（可选）

```bash
uv run python main.py --help
```

## 项目结构

```
├── api/                 # FastAPI 后端
│   └── services/insight/  # 评论洞察 / 回复 / 内容选题
├── web_templates/       # Web 页面
├── web_static/          # 前端静态资源
├── media_platform/      # 各平台爬虫
├── data/                # 采集与分析结果（默认忽略）
└── tests/               # 测试
```

## 测试

```bash
uv run pytest tests -q
```

## 免责声明

本工具仅供学习与研究使用。请遵守各平台服务条款，合理控制请求频率，**不得用于批量骚扰用户**。使用受控回复时请进一步确认频率与时段合规，并由使用者自行承担合规责任。

## License

MIT — 见 [LICENSE](./LICENSE)